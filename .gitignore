node_modules/
.svelte-kit/
.vercel
__pycache__/

# build output — written and read back inside build-brand-font.py in one run.
# the REGULAR ttf stays tracked: solve-wordmark.py reads it as a separate tool.
tools/pyre-display-700.ttf

# secrets: `vercel env pull` writes DATABASE_URL here for draft previews
.env*
!.env.example
.DS_Store

# episode media (raw recordings, renders, masters) is gigabytes and never goes
# in git. the scripts that make it are tracked in tools/episode/.
assets/ep*/
# speaker reference clips for diarization — voices stay out of a public repo
assets/speakers/
.env*.local
